{
  "name": "shape-motion-area",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "check": "node --check app.js && node --check site-header.js",
    "build": "mkdir -p dist && cp index.html styles.css app.js site-header.js dist/"
  }
}
